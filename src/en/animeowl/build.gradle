ext {
    extName = 'AnimeOwl (Dead)'
    extClass = '.AnimeOwl'
    extVersionCode = 32
}

apply from: "$rootDir/common.gradle"

dependencies {
    implementation(project(":lib:synchrony"))
    implementation(project(":lib:playlist-utils"))
}
